## 二次开发增强功能

本项目由 **神之翼工作室** 基于 MCSManager v10 二次开发，新增以下商用增强功能：

### 邮箱注册
- 管理员可在面板设置中配置 SMTP 邮件服务（支持 QQ邮箱、163、Gmail 等）
- 用户通过邮箱 + 密码 + 验证码注册账号，用户名即为邮箱地址
- 支持开放/关闭公开注册开关

### 本地兑换码系统
- 管理员可生成兑换码，设置时长、使用次数、关联套餐
- 普通用户输入兑换码即可自动开通实例（无需手动选择节点/分类）
- 兑换码仅在实例创建成功后才会被消耗，避免失败吞码
- 支持未使用兑换码导出为 TXT（全部导出 / 按套餐导出）

### 套餐管理
- 管理员可创建可复用的实例配置模板（镜像、内存、CPU、端口、环境变量等）
- 支持时长单位（小时/天/月/年/永久）
- 支持实例命名规则（前缀 + 后缀类型：用户名、哈希、递增、自定义）
- 生成兑换码时可直接选择套餐，自动填充配置

### 品牌定制
- 网站标题、Favicon、Logo、背景图自定义
- 品牌名称显示于登录页、注册页、导航栏
- 关于页显示品牌 GitHub、官网、联系邮箱

### 商用 UI
- 克制 SaaS 控制台风格，浅色低饱和，圆角卡片
- 全中文界面，面向国内用户优化

---

原始项目：MCSManager (https://github.com/MCSManager/MCSManager)
许可证：Apache 2.0
